{
  "name": "suivi-table-ronde",
  "version": "1.0.0",
  "description": "Outil communautaire pour préparer et suivre la table ronde",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/verify-build.js",
    "dev": "node scripts/serve.js",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --check \"**/*.{js,md,json,yml,yaml}\"",
    "format:write": "prettier --write \"**/*.{js,md,json,yml,yaml}\"",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
